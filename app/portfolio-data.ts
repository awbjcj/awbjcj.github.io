/**
 * Backward-compatible export surface.
 *
 * Editable website content lives in the purpose-specific files under
 * `app/content/`. New code should import from `app/content` directly.
 */
export * from "./content";
